#include <stdio.h>
#include <stdlib.h>
#include "vm.h"
// int main(int argc, char *argv[])
int main()
{
	// VirtualMachine vm = init_vm();
	// uint8_t text[] = {0x0, ROM_DATA_START, ROM_DATA_START << 8};
	// init_text(&vm, text, 4);
	return EXIT_SUCCESS;
}
